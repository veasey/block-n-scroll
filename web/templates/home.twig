{% extends "base.twig" %}

{% block title %}Block &amp; Scroll - Home{% endblock %}

{% block content %}
    <h3>Look up rules, track your team and run your leagues!</h3>
    <div class="primary-nav">
        <a href="/rules" class="primary-nav-link">Rules &amp; Stats</a>
        <a href="/team" class="primary-nav-link">Team Management</a>
        <a href="/leagues" class="primary-nav-link">League Tracker</a>
    </div>

    <h3>Latest Matches</h3>
    <table class="stacked">
        <thead>
            <th>Match Start Time</th>
            <th>Teams</th>
            <th>Status</th>
            <th>&nbsp;</th>
        </thead>
        {% for match in matches %}
            <tr>
                <td data-label="Start">
                    <div class="cell-wrap">
                        {{match.created_at|date("l j F Y \\a\\t H:i")}}
                    </div>
                </td>
                <td data-label="Teams">
                    <div class="cell-wrap">
                        {% include 'partials/home_team_name.twig' with {'match': match} %} ({{match.home_score ?? 0}}) vs 
                        {% include 'partials/away_team_name.twig' with {'match': match} %} ({{match.away_core ?? 0}})
                    </div>
                </td>
                <td data-label="Status">{{ match.status }}
                </td>
                <td>
                    <a href="/match/view/{{match.id}}">
                        View
                    </a>
                </td>
            </tr>
        {% endfor %}
    </table>

    <div class="news">
        <h2>Latest News</h2>
        <ul>
            <li><strong>12 June 2025:</strong> Block &amp; Scroll is now live! Explore the new platform and its features.</li>
            <li><strong>11 June 2025:</strong> You can now manage your teams and track your leagues with our easy-to-use tools.</li>
            <li><strong>10 June 2025:</strong> The Rules &amp; Stats section has been refreshed—take a look for the latest info.</li>
        </ul>
    </div>
{% endblock %}